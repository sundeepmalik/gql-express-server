const getAllCourses = (root) => new Promise((resolve, reject) => {

    const courses = [];
    
    return resolve(courses);
           
});

export default {
    Query: {
        courseList: getAllCourses
    }
};
